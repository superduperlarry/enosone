import { RoadmapPage } from "@/components/roadmap-page";

export default function IdentityPage() {
  return (
    <RoadmapPage
      feature="identity"
      title="Verified Identity"
      phase="Phase D"
      description="Verify once, disclose nothing — a privacy-preserving identity layer your agents can carry everywhere."
    />
  );
}
