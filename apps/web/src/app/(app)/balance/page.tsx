import { RoadmapPage } from "@/components/roadmap-page";

export default function BalancePage() {
  return (
    <RoadmapPage
      title="Balance"
      phase="Phase B"
      description="A Balance for every agent — fund it, move it between agents instantly, and let Enstack Routing find the best way to pay."
    />
  );
}
