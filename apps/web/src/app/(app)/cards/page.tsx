import { RoadmapPage } from "@/components/roadmap-page";

export default function CardsPage() {
  return (
    <RoadmapPage
      feature="cards"
      title="Agent Cards"
      phase="Phase C"
      description="A card for every agent, funded by its Balance — every authorization checked against your policy in real time."
    />
  );
}
