import { RoadmapPage } from "@/components/roadmap-page";

export default function BankPage() {
  return (
    <RoadmapPage
      title="Bank Account"
      phase="Phase E"
      description="A named account for every agent — receive into its Balance over local and international rails."
    />
  );
}
